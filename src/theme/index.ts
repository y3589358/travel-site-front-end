import {extendTheme} from '@chakra-ui/react';

export default extendTheme({
  styles: {
    global: {
      // styles for the `body`
      body: {
        bg: '#FAFAFA',
        color: '#4A4A4A',
        fontFamily: 'PingFang SC',
      },
    },
  },
});
